#include<stdio.h>

int main()
{
	int number, i;
	int array_main[10];
	int array_secondary[5];
	
	scanf("%d" ,&number);
	if (number > '100') && (number < '0')
	{
		println("Number must be 0<number<100");
	}
	for(i = 0; i <= 10; i++)
	{
		scanf("%d",&number_array);
		array_main[i] = number_array;
		
	}
}

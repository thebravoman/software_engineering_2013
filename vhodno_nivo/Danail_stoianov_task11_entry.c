#include <stdio.h>


int main()
{
	float x, i;
	printf("Enter x (0 < x <1): ");
	scanf("%f", &x);
	
	if((x > 0) && (x < 1))
	{	
		//printf("%f", x);
		for(i = 0; i < x; i++)
		{
			
		}
		
	}
	else
		return 1;
}

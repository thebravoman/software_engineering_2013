#include <stdio.h>

int main() {
int x, y, i, n, c, s arr[10];

while ((x<=0 || (x=>10)) {
printf("Vuvede x: ");
scanf("%d", &x);
}

i=0
n=0
while (i<10) {
	if (n%x == 0) {
		for (c=0, c=n, c++)
			s=c+s;
			arr[i]=s
			i=i+1
			}
		else n=n+1
			}
			
			
for (i=0; i<10, i++)
	printf(arr[i]);
}
